# Suppress "Could not reliably determine the server's fully qualified domain name" warning
ServerName localhost

# Pass Authorization header through Apache to PHP.
# Required for API key authentication — Apache + PHP-FPM strips it by default.
# See CLAUDE.md gotchas for context.
<Directory /var/www/html>
    CGIPassAuth on
</Directory>
